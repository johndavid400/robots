<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<%= hook :inside_head do %>
  <%= render :partial => 'shared/head'%>
<% end %>

<div id="header">
  <div class="title_image"> 
    <img id="title_bg" src="images/prototype_robotics/Logo_4.png" height="160px" />
  </div>
  <div class="log_in"> 
    <% if !current_user.nil? %>
      <%= image_tag avatar_url(current_user), :size => "20x20", :class => "avatar" %>  
      <% if current_user.has_role?("admin")  %>
        <%= link_to t('admin_dashboard'), admin_path, {:class => "login_links", :style => 'color:white'} %> 
       &nbsp; &nbsp; &nbsp;
      <% end %>
    <% end %>
    <% if !current_user.nil? %>
      <%= link_to t('my_account'), account_path, {:class => "login_links", :style => 'color:white'} %> 
       &nbsp; &nbsp; &nbsp; 
      <%= link_to t('logout'), destroy_user_session_path, {:class => "login_links", :style => 'color:white'} %>  
    <% else %>
      <%= link_to t('login'), new_user_session_path, {:class => "login_links", :style => 'color:white'} %>
    <% end %>
  </div>
  <div class="advertisement">
    <h2>Advertisement</h2>
  </div>
</div>

  <%= render :partial => 'shared/menu'%>

</head>
<%
if @body_class.nil?
  if content_for?(:sidebar)
    @body_class = 'two-col'
  else
    @body_class = 'one-col'
  end
end
 %>
<body class="<%= @body_class %>" id="<%= @body_id %>">
  <div id="header" class="container_bg">
    <div class="container">

  </div>
  </div>

  <div id="wrapper" class="container">
    <% if content_for?(:sidebar) %>
    <div id="sidebar">
      <%= hook :sidebar do %>
        <%= yield :sidebar %>
      <% end %>
    </div>
    <% end %>

    <div id="content">
      <%= breadcrumbs(@taxon) if @taxon %>
      <% if flash.notice %>
      <div class="flash notice"><%= flash.notice %></div>
      <% end %>
      <% if flash[:error] %>
      <div class="flash errors"><%= flash[:error] %></div>
      <% end %>

      <%= yield %>

    </div>
    <%= render :partial => 'shared/footer'%>
  </div>

</body>
</html>
